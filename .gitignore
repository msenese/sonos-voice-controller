config.py
__pycache__/
*.pyc
.venv/
venv/
*.wav
training_samples/
.DS_Store
sample_counts_baseline.json

# Transient model artifacts at the repo root (deployed to the Pi directly,
# not version-controlled) -- models/ is where tracked models live.
/sonos-model.eim
/sonos-model-pending.eim
/sonos-model-previous.eim
/sonos-model.meta.json
/sonos-model-previous.meta.json
